{% extends 'bootstrap_3_layout.html.twig' %}

{% block form_label %}
    {{ parent() }}

    {% if required %}
        <span class="required" title="{{ 'text.help.required'|trans }}">*</span>
    {% endif %}
{% endblock %}
