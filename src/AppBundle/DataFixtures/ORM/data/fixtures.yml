AppBundle\Entity\Category:
    childCategory{1..5}:
        name: <word()>
        description: <text(250)>
        parent: 0 # the main category is defined manually via DQL
    # subchild categories are defined in a second file
